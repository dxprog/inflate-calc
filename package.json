{
  "name": "inflate-calc",
  "version": "1.0.0",
  "description": "An infaltion calculator Alexa skill",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "rm -f lambda-package.zip && zip -r lambda-package.zip *"
  },
  "author": "Matt Hackmann",
  "license": "WTFPL",
  "dependencies": {
    "alexa-sdk": "^1.0.11"
  }
}
